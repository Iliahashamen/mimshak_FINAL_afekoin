<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary">

    <!-- Header row -->
    <TextView
        android:id="@+id/tvAfeklikerTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="AFEKLIKER"
        android:textSize="22sp"
        android:textStyle="bold"
        android:letterSpacing="0.1"
        android:textColor="@color/text_primary"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Timer ring area -->
    <ProgressBar
        android:id="@+id/timerProgress"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="240dp"
        android:layout_height="10dp"
        android:max="20"
        android:progress="20"
        android:progressTint="@color/accent_green"
        android:progressBackgroundTint="@color/bg_card"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvAfeklikerTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvAfeklikerTimer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0:20"
        android:textSize="36sp"
        android:textStyle="bold"
        android:textColor="@color/accent_green"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/timerProgress"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Taps counter -->
    <TextView
        android:id="@+id/tvAfeklikerTaps"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Taps: 0"
        android:textSize="20sp"
        android:textColor="@color/text_secondary"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvAfeklikerTimer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Tap button (glow effect via padding + CardView) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/logoCardContainer"
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:cardCornerRadius="100dp"
        app:cardElevation="24dp"
        app:cardBackgroundColor="#1A4D2E"
        app:layout_constraintTop_toBottomOf="@id/tvAfeklikerTaps"
        app:layout_constraintBottom_toTopOf="@id/btnAfeklikerDone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.5">

        <ImageButton
            android:id="@+id/btnAfekoinLogo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:contentDescription="Tap to earn"
            android:scaleType="centerCrop"
            android:src="@mipmap/afekoin_logo" />
    </androidx.cardview.widget.CardView>

    <Button
        android:id="@+id/btnAfeklikerDone"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_marginHorizontal="32dp"
        android:layout_marginBottom="32dp"
        android:text="FINISH &amp; CLAIM"
        android:textStyle="bold"
        android:backgroundTint="@color/accent_green"
        android:textColor="@color/bg_primary"
        app:cornerRadius="14dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
