rules_version = '2';
// העתק ל-Storage Rules בקונסול.
service firebase.storage {
  match /b/{bucket}/o {
    match /profile_images/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && fileName == request.auth.uid + '.jpg';
    }
  }
}
